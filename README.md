# SQL Practice – Chinook Database

This repo contains SQL queries I wrote while learning SQL for interview preparation, using the Chinook sample database.

## Topics covered
- Basic SELECT, WHERE, ORDER BY
- JOINs (INNER JOIN, Self JOIN)
- GROUP BY and aggregate functions
- Subqueries
- Window functions (RANK, LAG)
- Date functions
- CASE WHEN
- Mixed, multi-concept interview-style questions
